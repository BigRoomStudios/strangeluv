language: node_js

node_js:
  - "10"

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm run lint
  - npm run test
  - NODE_ENV=development npm run deploy
  - NODE_ENV=production npm run deploy
