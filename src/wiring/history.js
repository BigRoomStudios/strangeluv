/*
const CreateBrowserHistory = require('history/createBrowserHistory');
// https://github.com/ReactTraining/history#properties 
// https://github.com/ReactTraining/react-router/issues/4006
// no need for useRouterHistory anymore
const UseRouterHistory = require('react-router').useRouterHistory;

module.exports = UseRouterHistory(CreateBrowserHistory)({
    basename: __BASENAME__
});
*/